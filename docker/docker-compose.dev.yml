services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dev
    environment:
      - VITE_API_BASE_URL=/api
    ports:
      - "5173:5173"
    volumes:
      - ..:/app
      - /app/node_modules